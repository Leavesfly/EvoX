# EvoX 用户使用指南

<div align="center">

**🚀 EvoX 框架完整用户手册**

[![Java](https://img.shields.io/badge/Java-17+-orange.svg)](https://www.oracle.com/java/)
[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.2.5-brightgreen.svg)](https://spring.io/projects/spring-boot)
[![Spring AI](https://img.shields.io/badge/Spring%20AI-1.0.0--M1-blue.svg)](https://spring.io/projects/spring-ai)
[![License](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

**一份帮助您快速上手并深度使用 EvoX 智能代理框架的完整指南**

</div>

---

## 📋 目录

- [项目简介](#项目简介)
- [环境准备](#环境准备)
- [快速安装](#快速安装)
- [配置指南](#配置指南)
- [核心功能使用](#核心功能使用)
- [示例应用](#示例应用)
- [最佳实践](#最佳实践)
- [常见问题解答](#常见问题解答)
- [进阶使用](#进阶使用)
- [性能优化](#性能优化)
- [附录](#附录)

---

## 🎯 项目简介

### 什么是 EvoX？

EvoX 是一个**企业级智能代理（Agent）框架**，基于 Java 17、Spring Boot 3.2+ 和 Spring AI 1.0+ 构建。它为开发者提供了一套完整的工具和抽象层，用于快速构建具有自主学习、协同决策和工作流自动化能力的 AI 应用系统。

### 核心特点

| 特性 | 说明 |
|------|------|
| **企业级架构** | 基于 Spring 生态，分层清晰，依赖明确，适合大型项目 |
| **模块化设计** | 16+ 独立模块，按需引入，支持渐进式集成 |
| **多模型支持** | 统一抽象层，支持 OpenAI、阿里云通义、百度文心等多种 LLM |
| **响应式编程** | 基于 Project Reactor，支持异步、非阻塞调用 |
| **可扩展性强** | 丰富的扩展点，支持自定义 Agent、Action、Tool、优化器 |
| **生产就绪** | 完善的异常处理、日志记录、配置管理 |

### 适用场景

EvoX 特别适合以下应用场景：

| 场景类型 | 典型应用 | 推荐模块 |
|---------|---------|---------|
| **智能对话** | 客服机器人、虚拟助手、问答系统 | `evox-agents`, `evox-memory` |
| **知识管理** | 企业知识库、文档问答、智能搜索 | `evox-rag`, `evox-storage` |
| **流程自动化** | 业务流程编排、任务调度、审批流 | `evox-workflow`, `evox-hitl` |
| **多智能体协同** | 团队协作、辩论系统、共识决策 | `evox-frameworks` |
| **智能优化** | 提示词优化、工作流优化、超参调优 | `evox-optimizers` |
| **工具集成** | API 调用、数据处理、文件操作 | `evox-tools` |

### 架构概览

```
┌─────────────────────────────────────────────────────────────────┐
│                      应用层 (Applications)                       │
│                evox-examples / evox-benchmark                   │
├─────────────────────────────────────────────────────────────────┤
│                     框架层 (Frameworks)                          │
│                      evox-frameworks                            │
│                   (多智能体框架、辩论系统)                          │
├─────────────────────────────────────────────────────────────────┤
│                    高级业务层 (Advanced Services)                │
│  ┌──────────────┬──────────────┬──────────────────────────┐   │
│  │  Optimizers  │     HITL     │       Evaluators         │   │
│  │   优化器      │   人机协同    │        评估器            │   │
│  └──────────────┴──────────────┴──────────────────────────┘   │
├─────────────────────────────────────────────────────────────────┤
│                     业务层 (Business Logic)                     │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐    │
│  │   Agents    │  Workflow   │     RAG     │   Prompts   │    │
│  │   代理系统   │   工作流     │   检索增强   │  提示词管理  │    │
│  └─────────────┴─────────────┴─────────────┴─────────────┘    │
├─────────────────────────────────────────────────────────────────┤
│                    能力层 (Capabilities)                        │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐    │
│  │   Memory    │    Tools    │   Storage   │    Utils    │    │
│  │   记忆管理   │   工具集     │   存储适配   │   工具类     │    │
│  └─────────────┴─────────────┴─────────────┴─────────────┘    │
├─────────────────────────────────────────────────────────────────┤
│                     核心层 (Core Services)                      │
│  ┌────────────────┬────────────────┬────────────────────┐      │
│  │      Core      │     Models     │      Actions       │      │
│  │    核心抽象     │    模型适配     │     动作引擎        │      │
│  └────────────────┴────────────────┴────────────────────┘      │
├─────────────────────────────────────────────────────────────────┤
│                    基础设施层 (Infrastructure)                   │
│          Spring Boot 3.2+ / Spring AI 1.0+ / Reactor           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 💻 环境准备

### 系统要求

在开始使用 EvoX 之前，请确保您的开发环境满足以下要求：

| 项目 | 要求 | 验证命令 | 说明 |
|------|------|----------|------|
| **操作系统** | Windows / macOS / Linux | - | 任意现代操作系统 |
| **JDK** | 17 或更高版本 | `java -version` | 推荐使用 OpenJDK 或 Oracle JDK |
| **Maven** | 3.8 或更高版本 | `mvn -version` | 用于项目构建和依赖管理 |
| **IDE** | IntelliJ IDEA / VS Code | - | 推荐 IntelliJ IDEA Ultimate |
| **Git** | 2.0+ | `git --version` | 用于版本控制 |

### 安装 JDK 17

#### macOS (使用 Homebrew)
```bash
# 安装 OpenJDK 17
brew install openjdk@17

# 配置环境变量
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc

# 验证安装
java -version
```

#### Linux (Ubuntu/Debian)
```bash
# 安装 OpenJDK 17
sudo apt update
sudo apt install openjdk-17-jdk

# 验证安装
java -version
```

#### Windows
1. 下载 OpenJDK 17: https://adoptium.net/
2. 安装并配置 `JAVA_HOME` 环境变量
3. 将 `%JAVA_HOME%\bin` 添加到 `PATH`
4. 打开命令提示符验证: `java -version`

### 安装 Maven

#### macOS (使用 Homebrew)
```bash
brew install maven
mvn -version
```

#### Linux (Ubuntu/Debian)
```bash
sudo apt install maven
mvn -version
```

#### Windows
1. 下载 Maven: https://maven.apache.org/download.cgi
2. 解压到目标目录（如 `C:\Program Files\Apache\Maven`）
3. 配置 `MAVEN_HOME` 环境变量
4. 将 `%MAVEN_HOME%\bin` 添加到 `PATH`
5. 验证: `mvn -version`

### 获取 LLM API Key

EvoX 支持多种 LLM 提供商，您需要至少获取一个 API Key：

#### OpenAI API Key（推荐）
1. 访问 https://platform.openai.com/
2. 注册账号并登录
3. 进入 API Keys 页面
4. 创建新的 API Key
5. 保存 Key（格式：`sk-xxxxxxxxxxxx`）

#### 阿里云通义千问 API Key
1. 访问 https://dashscope.aliyun.com/
2. 注册并实名认证
3. 创建 API Key
4. 保存 Key 和 Secret

#### 百度文心一言 API Key
1. 访问 https://cloud.baidu.com/product/wenxinworkshop
2. 注册并创建应用
3. 获取 API Key 和 Secret Key

---

## 🚀 快速安装

### 步骤 1: 克隆项目

```bash
# 克隆项目到本地
git clone https://github.com/your-org/evox.git

# 进入项目目录
cd evox
```

### 步骤 2: 配置 Maven 镜像（可选，加速依赖下载）

如果您在国内，建议配置阿里云 Maven 镜像以加速依赖下载。

编辑 `~/.m2/settings.xml`（如果文件不存在则创建）：

```xml
<settings>
  <mirrors>
    <mirror>
      <id>aliyun</id>
      <mirrorOf>central</mirrorOf>
      <name>Aliyun Maven</name>
      <url>https://maven.aliyun.com/repository/public</url>
    </mirror>
  </mirrors>
</settings>
```

### 步骤 3: 编译项目

```bash
# 快速编译（跳过测试，推荐首次安装使用）
mvn clean install -DskipTests

# 完整编译（包含测试，需要 5-10 分钟）
mvn clean install
```

**编译过程说明：**
- ✅ 下载所有依赖（Spring Boot、Spring AI 等）
- ✅ 编译所有 16 个模块
- ✅ 打包生成 JAR 文件
- ✅ 安装到本地 Maven 仓库

**预期输出：**
```
[INFO] ------------------------------------------------------------------------
[INFO] Reactor Summary for evox 1.0.0-SNAPSHOT:
[INFO] 
[INFO] evox .............................................. SUCCESS
[INFO] evox-core ......................................... SUCCESS
[INFO] evox-models ....................................... SUCCESS
[INFO] evox-actions ...................................... SUCCESS
[INFO] evox-storage ...................................... SUCCESS
[INFO] evox-memory ....................................... SUCCESS
[INFO] evox-tools ........................................ SUCCESS
[INFO] evox-agents ....................................... SUCCESS
[INFO] evox-workflow ..................................... SUCCESS
[INFO] evox-examples ..................................... SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
```

### 步骤 4: 验证安装

```bash
# 查看编译产物
ls -la ~/.m2/repository/io/leavesfly/evox/

# 应该看到所有模块的 JAR 文件
```

---

## ⚙️ 配置指南

### 配置 API Key

EvoX 支持三种配置方式，按优先级从高到低排列：

#### 方式 1: 环境变量（推荐）

**macOS / Linux:**
```bash
# 临时配置（当前会话有效）
export OPENAI_API_KEY="sk-your-api-key-here"
export OPENAI_MODEL="gpt-4o-mini"

# 永久配置（添加到 ~/.bashrc 或 ~/.zshrc）
echo 'export OPENAI_API_KEY="sk-your-api-key-here"' >> ~/.zshrc
echo 'export OPENAI_MODEL="gpt-4o-mini"' >> ~/.zshrc
source ~/.zshrc
```

**Windows (PowerShell):**
```powershell
# 临时配置
$env:OPENAI_API_KEY="sk-your-api-key-here"
$env:OPENAI_MODEL="gpt-4o-mini"

# 永久配置（通过系统环境变量设置）
[System.Environment]::SetEnvironmentVariable('OPENAI_API_KEY', 'sk-your-api-key-here', 'User')
```

#### 方式 2: 配置文件

创建或编辑 `evox-application/evox-examples/src/main/resources/application.yml`：

```yaml
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:sk-your-default-key}
      base-url: https://api.openai.com
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7
          max-tokens: 2000
```

#### 方式 3: 程序代码

```java
import io.leavesfly.evox.models.config.LLMConfig;
import io.leavesfly.evox.models.openai.OpenAILLM;

LLMConfig config = LLMConfig.builder()
    .apiKey("sk-your-api-key-here")
    .model("gpt-4o-mini")
    .temperature(0.7)
    .maxTokens(1000)
    .build();

OpenAILLM llm = new OpenAILLM(config);
```

### 验证配置

```bash
# 验证环境变量
echo $OPENAI_API_KEY

# 测试 API 连接（使用 curl）
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"
```

### 多环境配置

EvoX 支持 Spring Boot 的多环境配置机制：

**开发环境 (application-dev.yml):**
```yaml
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.9  # 开发环境可以更随机
```

**生产环境 (application-prod.yml):**
```yaml
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o
          temperature: 0.3  # 生产环境更确定性
```

**激活特定环境:**
```bash
# 使用开发环境
mvn exec:java -Dspring.profiles.active=dev

# 使用生产环境
mvn exec:java -Dspring.profiles.active=prod
```

---

## 🎓 核心功能使用

### 1️⃣ 创建第一个聊天机器人

这是最简单的入门示例，展示如何创建一个基础的 AI 对话程序。

**创建文件:** `QuickStart.java`

```java
import io.leavesfly.evox.agents.base.Agent;
import io.leavesfly.evox.models.openai.OpenAILLM;
import io.leavesfly.evox.models.config.LLMConfig;
import io.leavesfly.evox.core.message.Message;
import io.leavesfly.evox.core.message.MessageType;

import java.util.Collections;

public class QuickStart {
    public static void main(String[] args) {
        // 1. 配置 LLM
        LLMConfig config = LLMConfig.builder()
            .apiKey(System.getenv("OPENAI_API_KEY"))
            .model("gpt-4o-mini")
            .temperature(0.7)
            .maxTokens(1000)
            .build();
        
        // 2. 创建 LLM 实例
        OpenAILLM llm = new OpenAILLM(config);
        
        // 3. 创建 Agent
        Agent agent = Agent.builder()
            .name("QuickStartBot")
            .description("A simple AI assistant")
            .llm(llm)
            .build();
        
        // 4. 发送消息
        Message userMessage = Message.builder()
            .content("你好！请简单介绍一下 EvoX 框架。")
            .messageType(MessageType.USER)
            .build();
        
        // 5. 获取回复
        Message response = agent.execute("chat", 
            Collections.singletonList(userMessage));
        
        // 6. 输出结果
        System.out.println("User: " + userMessage.getContent());
        System.out.println("AI: " + response.getContent());
    }
}
```

**运行程序:**
```bash
# 编译
javac -cp "target/*:~/.m2/repository/..." QuickStart.java

# 运行
java -cp "target/*:~/.m2/repository/..." QuickStart
```

**预期输出:**
```
User: 你好！请简单介绍一下 EvoX 框架。
AI: 你好！EvoX 是一个基于 Spring Boot 和 Spring AI 的企业级智能代理框架，它提供了完整的多智能体协同、工作流编排、记忆管理等能力...
```

### 2️⃣ 添加记忆功能

让您的 Agent 能够记住对话历史。

```java
import io.leavesfly.evox.memory.shortterm.ShortTermMemory;

public class MemoryChatBot {
    public static void main(String[] args) {
        // 1. 创建短期记忆
        ShortTermMemory memory = ShortTermMemory.builder()
            .capacity(100)        // 最大容量 100 条消息
            .windowSize(10)       // 滑动窗口大小 10
            .build();
        
        // 2. 创建带记忆的 Agent
        Agent agent = Agent.builder()
            .name("MemoryBot")
            .llm(new OpenAILLM(config))
            .memory(memory)       // 添加记忆
            .build();
        
        // 3. 多轮对话
        String[] questions = {
            "我叫张三",
            "我最喜欢的颜色是蓝色",
            "你还记得我叫什么名字吗？",
            "我喜欢什么颜色？"
        };
        
        for (String question : questions) {
            Message msg = Message.builder()
                .content(question)
                .messageType(MessageType.USER)
                .build();
            
            // 保存用户消息到记忆
            memory.addMessage(msg);
            
            // 获取历史上下文
            List<Message> context = memory.getLatestMessages(5);
            
            // 发送带上下文的请求
            Message response = agent.execute("chat", context);
            
            // 保存 AI 回复到记忆
            memory.addMessage(response);
            
            System.out.println("Q: " + question);
            System.out.println("A: " + response.getContent());
            System.out.println();
        }
    }
}
```

**运行结果:**
```
Q: 我叫张三
A: 你好，张三！很高兴认识你。

Q: 我最喜欢的颜色是蓝色
A: 好的，我记住了，你喜欢蓝色。

Q: 你还记得我叫什么名字吗？
A: 当然记得，你叫张三。

Q: 我喜欢什么颜色？
A: 你最喜欢的颜色是蓝色。
```

### 3️⃣ 集成外部工具

让 Agent 具备调用外部工具的能力（文件操作、HTTP 请求、搜索等）。

```java
import io.leavesfly.evox.tools.base.Toolkit;
import io.leavesfly.evox.tools.file.FileSystemTool;
import io.leavesfly.evox.tools.http.HttpTool;
import io.leavesfly.evox.tools.search.WebSearchTool;
import io.leavesfly.evox.agents.specialized.ToolAgent;

public class ToolIntegrationExample {
    public static void main(String[] args) {
        // 1. 创建工具集
        Toolkit toolkit = new Toolkit();
        toolkit.addTool(new FileSystemTool());     // 文件操作
        toolkit.addTool(new HttpTool());          // HTTP 请求
        toolkit.addTool(new WebSearchTool());     // 网络搜索
        
        // 2. 创建 ToolAgent
        ToolAgent toolAgent = ToolAgent.builder()
            .name("ToolBot")
            .llm(new OpenAILLM(config))
            .toolkit(toolkit)
            .build();
        
        // 3. 使用工具
        Message request = Message.builder()
            .content("请搜索最新的 AI 新闻，并保存到 news.txt 文件")
            .messageType(MessageType.USER)
            .build();
        
        Message response = toolAgent.execute("use-tool", 
            Collections.singletonList(request));
        
        System.out.println("Result: " + response.getContent());
    }
}
```

### 4️⃣ 构建工作流

创建复杂的多步骤工作流程。

```java
import io.leavesfly.evox.workflow.base.Workflow;
import io.leavesfly.evox.workflow.graph.WorkflowGraph;
import io.leavesfly.evox.workflow.graph.WorkflowNode;
import io.leavesfly.evox.workflow.graph.WorkflowNode.NodeType;

public class WorkflowExample {
    public static void main(String[] args) {
        // 1. 创建工作流节点
        WorkflowNode step1 = WorkflowNode.builder()
            .nodeId("analyze")
            .name("分析问题")
            .nodeType(NodeType.ACTION)
            .action(new AnalyzeAction())
            .build();
        
        WorkflowNode step2 = WorkflowNode.builder()
            .nodeId("search")
            .name("搜索信息")
            .nodeType(NodeType.ACTION)
            .action(new SearchAction())
            .build();
        
        WorkflowNode step3 = WorkflowNode.builder()
            .nodeId("summarize")
            .name("总结答案")
            .nodeType(NodeType.ACTION)
            .action(new SummarizeAction())
            .build();
        
        // 2. 构建工作流图
        WorkflowGraph graph = new WorkflowGraph();
        graph.addNode(step1);
        graph.addNode(step2);
        graph.addNode(step3);
        
        // 定义节点依赖关系
        graph.addEdge("analyze", "search");     // step1 -> step2
        graph.addEdge("search", "summarize");   // step2 -> step3
        
        // 3. 创建工作流
        Workflow workflow = Workflow.builder()
            .name("QA-Workflow")
            .description("问答系统工作流")
            .graph(graph)
            .llm(new OpenAILLM(config))
            .build();
        
        // 4. 执行工作流
        Map<String, Object> inputs = Map.of(
            "question", "什么是人工智能？"
        );
        
        Map<String, Object> result = workflow.execute(inputs);
        System.out.println("结果: " + result.get("answer"));
    }
}
```

### 5️⃣ RAG 知识库问答

构建基于文档的智能问答系统。

```java
import io.leavesfly.evox.rag.base.RAGEngine;
import io.leavesfly.evox.rag.loader.DocumentLoader;
import io.leavesfly.evox.rag.retriever.VectorRetriever;
import io.leavesfly.evox.rag.config.RAGConfig;

public class RAGExample {
    public static void main(String[] args) {
        // 1. 加载文档
        DocumentLoader loader = new DocumentLoader();
        List<Document> documents = loader.loadDirectory("./docs");
        System.out.println("已加载 " + documents.size() + " 个文档");
        
        // 2. 配置 RAG 引擎
        RAGConfig config = RAGConfig.builder()
            .chunkSize(500)          // 分块大小
            .chunkOverlap(50)        // 重叠大小
            .topK(3)                 // 检索 Top-K
            .similarityThreshold(0.7) // 相似度阈值
            .build();
        
        // 3. 创建 RAG 引擎
        RAGEngine ragEngine = RAGEngine.builder()
            .config(config)
            .llm(new OpenAILLM(llmConfig))
            .vectorStore(new InMemoryVectorStore())
            .build();
        
        // 4. 索引文档
        ragEngine.indexDocuments(documents);
        System.out.println("文档索引完成");
        
        // 5. 执行问答
        String question = "EvoX 框架的主要特性是什么？";
        RAGResult result = ragEngine.query(question);
        
        System.out.println("问题: " + question);
        System.out.println("答案: " + result.getAnswer());
        System.out.println("参考文档: " + result.getReferences());
    }
}
```

---

## 📚 示例应用

### 运行示例程序

EvoX 提供了多个开箱即用的完整示例：

```bash
# 进入示例目录
cd evox-application/evox-examples

# 方式 1: 使用便捷脚本（推荐）
./run-examples.sh

# 方式 2: 直接运行特定示例
mvn exec:java -Dexec.mainClass="io.leavesfly.evox.examples.SimpleChatBot"
mvn exec:java -Dexec.mainClass="io.leavesfly.evox.examples.ComprehensiveChatBot"
mvn exec:java -Dexec.mainClass="io.leavesfly.evox.examples.WorkflowDemo"
```

### 示例列表

| 示例名称 | 功能说明 | 难度 | 代码位置 |
|---------|---------|------|----------|
| **QuickStart** | 5分钟快速入门 | ⭐ | `examples/QuickStart.java` |
| **SimpleChatBot** | 基础聊天机器人 | ⭐ | `examples/SimpleChatBot.java` |
| **MemoryAgentExample** | 带记忆的对话 | ⭐⭐ | `examples/MemoryAgentExample.java` |
| **ToolsExample** | 工具集成示例 | ⭐⭐ | `examples/ToolsExample.java` |
| **ComprehensiveChatBot** | 综合聊天系统 | ⭐⭐⭐ | `examples/ComprehensiveChatBot.java` |
| **WorkflowDemo** | 工作流编排 | ⭐⭐⭐ | `examples/WorkflowDemo.java` |
| **ActionAgentExample** | 动作执行示例 | ⭐⭐ | `examples/ActionAgentExample.java` |
| **BenchmarkExample** | 性能测试 | ⭐⭐⭐ | `examples/BenchmarkExample.java` |

### 示例详解：SimpleChatBot

**功能特性:**
- ✅ 单个智能体（QuestionAgent）
- ✅ 短期记忆（保持对话历史）
- ✅ 工具集成（文件、HTTP、搜索）
- ✅ 简单易懂的代码结构

**架构图:**
```
用户输入 → QuestionAgent → Tools(可选) → LLM生成 → 响应输出
                ↓
            ShortTermMemory
```

**运行命令:**
```bash
mvn exec:java -Dexec.mainClass="io.leavesfly.evox.examples.SimpleChatBot"
```

### 示例详解：ComprehensiveChatBot

**功能特性:**
- ✅ 多智能体协同（RouterAgent + ToolAgent + ChatAgent）
- ✅ 智能路由（自动选择合适的 Agent）
- ✅ 记忆管理（短期记忆 + 滑动窗口）
- ✅ 完整的对话流程

**架构图:**
```
用户输入 → RouterAgent (决策) → ToolAgent (工具处理)
                ↓                      ↓
           ChatAgent (普通聊天)  ←  AgentManager
                ↓
          ShortTermMemory
                ↓
            响应输出
```

**运行命令:**
```bash
mvn exec:java -Dexec.mainClass="io.leavesfly.evox.examples.ComprehensiveChatBot"
```

---

## 🏆 最佳实践

### 1. 项目结构建议

```
my-evox-app/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/mycompany/
│   │   │       ├── agent/          # 自定义 Agent
│   │   │       ├── action/         # 自定义 Action
│   │   │       ├── tool/           # 自定义工具
│   │   │       ├── workflow/       # 工作流定义
│   │   │       └── Application.java
│   │   └── resources/
│   │       ├── application.yml     # 配置文件
│   │       └── prompts/           # 提示词模板
│   └── test/
│       └── java/                   # 测试代码
├── pom.xml
└── README.md
```

### 2. 依赖管理

**推荐的 pom.xml 配置:**

```xml
<dependencies>
    <!-- EvoX 核心依赖 -->
    <dependency>
        <groupId>io.leavesfly.evox</groupId>
        <artifactId>evox-agents</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </dependency>
    
    <!-- 根据需要添加其他模块 -->
    <dependency>
        <groupId>io.leavesfly.evox</groupId>
        <artifactId>evox-workflow</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </dependency>
    
    <dependency>
        <groupId>io.leavesfly.evox</groupId>
        <artifactId>evox-rag</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </dependency>
</dependencies>
```

### 3. 代码规范

**命名规范:**
```java
// 类名：大驼峰
public class AgentManager { }

// 方法名：小驼峰
public void executeWorkflow() { }

// 常量：全大写+下划线
public static final String DEFAULT_MODEL = "gpt-4o-mini";

// 变量：小驼峰
private String apiKey;
```

**注释规范:**
```java
/**
 * Agent 管理器
 * 
 * <p>提供 Agent 的注册、发现和生命周期管理功能</p>
 * 
 * @author Your Name
 * @since 1.0.0
 */
public class AgentManager {
    
    /**
     * 注册一个 Agent
     * 
     * @param agent 要注册的 Agent 实例
     * @throws IllegalArgumentException 如果 agent 为 null
     */
    public void registerAgent(Agent agent) {
        // 实现代码
    }
}
```

### 4. 异常处理

```java
try {
    Message response = agent.execute("chat", messages);
} catch (AgentException e) {
    log.error("Agent execution failed: {}", e.getMessage());
    // 实现降级策略
    response = fallbackResponse();
} catch (Exception e) {
    log.error("Unexpected error", e);
    throw new ApplicationException("Service unavailable", e);
}
```

### 5. 日志管理

```java
import lombok.extern.slf4j.Slf4j;

@Slf4j
public class MyService {
    public void doSomething() {
        log.debug("Starting operation with param: {}", param);
        log.info("Operation completed successfully");
        log.warn("Resource usage high: {}%", usage);
        log.error("Operation failed", exception);
    }
}
```

### 6. 配置管理

**使用 Spring Boot 配置:**
```yaml
evox:
  llm:
    provider: openai
    api-key: ${OPENAI_API_KEY}
    model: gpt-4o-mini
    temperature: 0.7
    max-tokens: 2000
  
  memory:
    type: short-term
    capacity: 100
    window-size: 10
  
  workflow:
    max-nodes: 50
    timeout: 30000  # 30 seconds
```

---

## ❓ 常见问题解答

### Q1: 编译失败怎么办？

**问题:** 执行 `mvn clean install` 时报错

**解决方案:**
```bash
# 1. 检查 Java 版本
java -version  # 应该是 17+

# 2. 检查 Maven 版本
mvn -version   # 应该是 3.8+

# 3. 清理并重新编译
mvn clean
rm -rf ~/.m2/repository/io/leavesfly/evox
mvn install -U -DskipTests

# 4. 如果还是失败，查看详细日志
mvn install -X
```

### Q2: API Key 错误怎么办？

**问题:** `Unauthorized` 或 `Invalid API Key`

**解决方案:**
```bash
# 1. 检查环境变量
echo $OPENAI_API_KEY

# 2. 验证 Key 格式（应该以 sk- 开头）
# 正确: sk-xxxxxxxxxxxx
# 错误: xxxxxxxxxxxx

# 3. 测试 API Key
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer $OPENAI_API_KEY"

# 4. 重新设置
export OPENAI_API_KEY="sk-your-correct-key"
```

### Q3: 依赖下载慢怎么办？

**解决方案:** 配置国内 Maven 镜像

编辑 `~/.m2/settings.xml`：
```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

### Q4: 如何切换 LLM 提供商？

**从 OpenAI 切换到阿里云通义:**

```java
// 原来的代码
OpenAILLM llm = new OpenAILLM(openaiConfig);

// 修改为
QianWenLLM llm = new QianWenLLM(qianwenConfig);

// Agent 代码无需修改
Agent agent = Agent.builder()
    .llm(llm)  // 统一的 LLM 接口
    .build();
```

### Q5: 记忆没有保存怎么办？

**问题:** 对话历史没有保留

**检查清单:**
- ✅ 是否创建了 Memory 实例
- ✅ 是否将 Memory 添加到 Agent
- ✅ 是否在每次对话后调用 `memory.addMessage()`
- ✅ Memory 容量是否足够

**正确示例:**
```java
// 1. 创建并设置 Memory
ShortTermMemory memory = ShortTermMemory.builder()
    .capacity(100)
    .build();

agent.setMemory(memory);

// 2. 每次对话后保存
Message userMsg = ...;
memory.addMessage(userMsg);

Message aiResponse = agent.execute("chat", messages);
memory.addMessage(aiResponse);  // 重要！
```

### Q6: 工具调用没有生效？

**问题:** Agent 没有使用工具

**排查步骤:**
1. 确认工具已添加到 Toolkit
2. 确认 Agent 实现了工具调用逻辑
3. 检查用户输入是否包含触发词
4. 查看日志确认工具是否被识别

**调试示例:**
```java
// 启用详细日志
log.setLevel(Level.DEBUG);

// 检查工具注册
toolkit.listTools().forEach(tool -> 
    log.debug("Registered tool: {}", tool.getName())
);

// 检查工具调用
toolAgent.execute("use-tool", messages);
```

### Q7: 如何处理 LLM 调用超时？

**配置超时时间:**
```java
LLMConfig config = LLMConfig.builder()
    .apiKey(apiKey)
    .timeout(Duration.ofSeconds(30))  // 设置 30 秒超时
    .build();
```

**使用重试机制:**
```java
@Retryable(
    value = {TimeoutException.class},
    maxAttempts = 3,
    backoff = @Backoff(delay = 1000)
)
public Message callLLM(List<Message> messages) {
    return agent.execute("chat", messages);
}
```

### Q8: 如何调试工作流？

**启用工作流日志:**
```yaml
logging:
  level:
    io.leavesfly.evox.workflow: DEBUG
```

**查看工作流执行状态:**
```java
WorkflowExecution execution = workflow.execute(inputs);

// 查看每个节点的状态
execution.getNodes().forEach(node -> {
    log.info("Node: {}, Status: {}", 
        node.getName(), node.getStatus());
});
```

---

## 🚀 进阶使用

### 1. 自定义 Agent

创建满足特定需求的自定义 Agent：

```java
public class CustomAgent extends Agent {
    
    private final CustomService customService;
    
    public CustomAgent(CustomService service) {
        super();
        this.customService = service;
        
        // 添加自定义 Action
        addAction(new CustomAction(service));
    }
    
    @Override
    public Message execute(String actionName, List<Message> messages) {
        // 添加前置处理逻辑
        preProcess(messages);
        
        // 调用父类方法
        Message result = super.execute(actionName, messages);
        
        // 添加后置处理逻辑
        postProcess(result);
        
        return result;
    }
    
    private void preProcess(List<Message> messages) {
        // 自定义前置处理
        log.debug("Pre-processing {} messages", messages.size());
    }
    
    private void postProcess(Message result) {
        // 自定义后置处理
        customService.logResult(result);
    }
}
```

### 2. 自定义 Action

创建专门的动作处理器：

```java
public class CustomAction extends Action {
    
    @Override
    public ActionOutput execute(ActionInput input) {
        try {
            // 1. 验证输入
            validateInput(input);
            
            // 2. 执行业务逻辑
            String result = processLogic(input);
            
            // 3. 返回结果
            return ActionOutput.builder()
                .success(true)
                .data(result)
                .build();
                
        } catch (Exception e) {
            log.error("Action execution failed", e);
            return ActionOutput.builder()
                .success(false)
                .error(e.getMessage())
                .build();
        }
    }
    
    private void validateInput(ActionInput input) {
        if (input == null || input.getContent() == null) {
            throw new IllegalArgumentException("Invalid input");
        }
    }
    
    private String processLogic(ActionInput input) {
        // 实现具体业务逻辑
        return "Processed: " + input.getContent();
    }
}
```

### 3. 自定义工具

扩展工具能力：

```java
public class CustomTool implements BaseTool {
    
    @Override
    public String getName() {
        return "custom_tool";
    }
    
    @Override
    public String getDescription() {
        return "A custom tool for specific tasks";
    }
    
    @Override
    public ToolResult execute(Map<String, Object> parameters) {
        try {
            // 1. 解析参数
            String param1 = (String) parameters.get("param1");
            Integer param2 = (Integer) parameters.get("param2");
            
            // 2. 执行工具逻辑
            String result = doWork(param1, param2);
            
            // 3. 返回结果
            return ToolResult.builder()
                .success(true)
                .result(result)
                .build();
                
        } catch (Exception e) {
            return ToolResult.builder()
                .success(false)
                .error(e.getMessage())
                .build();
        }
    }
    
    private String doWork(String param1, Integer param2) {
        // 实现工具的核心功能
        return String.format("Processed %s with %d", param1, param2);
    }
}
```

### 4. 多智能体协同

使用辩论框架实现多 Agent 协同：

```java
import io.leavesfly.evox.frameworks.debate.MultiAgentDebate;

public class DebateExample {
    public static void main(String[] args) {
        // 1. 创建多个观点不同的 Agent
        Agent optimist = Agent.builder()
            .name("Optimist")
            .systemPrompt("你是一个乐观主义者，总是看到事物的积极面")
            .llm(new OpenAILLM(config))
            .build();
        
        Agent pessimist = Agent.builder()
            .name("Pessimist")
            .systemPrompt("你是一个悲观主义者，总是考虑潜在风险")
            .llm(new OpenAILLM(config))
            .build();
        
        Agent realist = Agent.builder()
            .name("Realist")
            .systemPrompt("你是一个现实主义者，客观分析利弊")
            .llm(new OpenAILLM(config))
            .build();
        
        // 2. 创建辩论框架
        MultiAgentDebate debate = MultiAgentDebate.builder()
            .agents(Arrays.asList(optimist, pessimist, realist))
            .maxRounds(3)
            .moderator(new OpenAILLM(config))
            .build();
        
        // 3. 开始辩论
        String topic = "AI 是否会取代人类的大部分工作？";
        DebateResult result = debate.startDebate(topic);
        
        // 4. 查看结果
        System.out.println("辩题: " + topic);
        System.out.println("辩论轮次: " + result.getRounds());
        System.out.println("最终结论: " + result.getConclusion());
    }
}
```

### 5. 提示词优化

使用优化器自动优化提示词：

```java
import io.leavesfly.evox.optimizers.TextGrad;

public class PromptOptimization {
    public static void main(String[] args) {
        // 1. 准备训练数据
        List<TrainingSample> samples = Arrays.asList(
            new TrainingSample("What is AI?", "AI is artificial intelligence..."),
            new TrainingSample("How does ML work?", "Machine learning works by...")
        );
        
        // 2. 创建优化器
        OptimizerConfig config = OptimizerConfig.builder()
            .learningRate(0.1)
            .batchSize(4)
            .maxIterations(10)
            .build();
        
        TextGrad optimizer = new TextGrad(config);
        
        // 3. 定义评估函数
        EvaluationFunction evalFunc = (prompt, samples) -> {
            double score = 0.0;
            for (TrainingSample sample : samples) {
                // 使用提示词生成回答
                String answer = generateAnswer(prompt, sample.getQuestion());
                // 计算与期望答案的相似度
                score += calculateSimilarity(answer, sample.getExpectedAnswer());
            }
            return score / samples.size();
        };
        
        // 4. 优化提示词
        String initialPrompt = "You are a helpful AI assistant.";
        OptimizationResult result = optimizer.optimize(
            initialPrompt, 
            samples, 
            evalFunc
        );
        
        System.out.println("原始提示词: " + initialPrompt);
        System.out.println("优化后: " + result.getBestPrompt());
        System.out.println("性能提升: " + result.getImprovement() + "%");
    }
}
```

---

## ⚡ 性能优化

### 1. 连接池配置

优化 HTTP 连接池以提升并发性能：

```yaml
evox:
  llm:
    openai:
      connection-pool:
        max-total: 200
        max-per-route: 50
        connection-timeout: 5000
        read-timeout: 30000
```

### 2. 缓存策略

使用缓存减少重复的 LLM 调用：

```java
@Cacheable(value = "llm-responses", key = "#messages.hashCode()")
public Message callLLM(List<Message> messages) {
    return agent.execute("chat", messages);
}
```

### 3. 异步处理

使用 Reactor 进行异步处理：

```java
import reactor.core.publisher.Mono;

public Mono<Message> executeAsync(List<Message> messages) {
    return Mono.fromCallable(() -> agent.execute("chat", messages))
        .subscribeOn(Schedulers.boundedElastic());
}
```

### 4. 批处理

批量处理请求以提高吞吐量：

```java
public List<Message> batchProcess(List<List<Message>> messageBatches) {
    return messageBatches.parallelStream()
        .map(messages -> agent.execute("chat", messages))
        .collect(Collectors.toList());
}
```

### 5. 监控与调优

启用性能监控：

```yaml
management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true
```

---

## 📎 附录

### A. 模块列表

| 模块 | 功能描述 | 依赖关系 |
|------|---------|---------|
| `evox-core` | 核心抽象、消息模型 | 无 |
| `evox-models` | LLM 模型适配 | evox-core |
| `evox-actions` | Action 执行引擎 | evox-core, evox-models |
| `evox-storage` | 存储适配层 | evox-core |
| `evox-memory` | 记忆管理 | evox-core, evox-storage |
| `evox-tools` | 工具集 | evox-core |
| `evox-utils` | 工具类 | 无 |
| `evox-agents` | 智能代理 | evox-core, evox-models, evox-actions, evox-tools |
| `evox-frameworks` | 多智能体框架 | evox-core, evox-agents |
| `evox-workflow` | 工作流引擎 | evox-core, evox-models, evox-memory, evox-storage |
| `evox-rag` | RAG 引擎 | evox-core, evox-models, evox-storage |
| `evox-optimizers` | 优化器 | evox-core, evox-models, evox-agents, evox-workflow |
| `evox-hitl` | 人机协同 | evox-core, evox-agents, evox-workflow |
| `evox-examples` | 示例应用 | 所有模块 |
| `evox-benchmark` | 性能测试 | evox-core |
| `evox-spring-boot-starter` | Spring Boot 自动配置 | evox-core, evox-agents |

### B. 常用命令

```bash
# 项目构建
mvn clean install                    # 完整构建
mvn clean install -DskipTests        # 跳过测试
mvn clean install -U                 # 强制更新依赖

# 运行示例
mvn exec:java -Dexec.mainClass="..."

# 运行测试
mvn test                             # 运行所有测试
mvn test -Dtest=ClassName            # 运行特定测试

# 查看依赖
mvn dependency:tree                  # 依赖树
mvn dependency:analyze               # 依赖分析

# 打包
mvn package                          # 打包项目
mvn package -DskipTests              # 打包跳过测试
```

### C. 配置参数参考

```yaml
evox:
  # LLM 配置
  llm:
    provider: openai                 # openai, qianwen, ernie
    api-key: ${OPENAI_API_KEY}
    model: gpt-4o-mini
    temperature: 0.7                 # 0.0-2.0
    max-tokens: 2000
    timeout: 30000                   # 毫秒
  
  # 记忆配置
  memory:
    type: short-term                 # short-term, long-term
    capacity: 100
    window-size: 10
  
  # 工作流配置
  workflow:
    max-nodes: 50
    timeout: 30000
    enable-cache: true
  
  # RAG 配置
  rag:
    chunk-size: 500
    chunk-overlap: 50
    top-k: 3
    similarity-threshold: 0.7
```

### D. 技术栈版本

| 技术 | 版本 | 用途 |
|------|------|------|
| Java | 17+ | 编程语言 |
| Spring Boot | 3.2.5 | 应用框架 |
| Spring AI | 1.0.0-M1 | AI 集成 |
| Project Reactor | 3.6.x | 响应式编程 |
| Jackson | 2.15.4 | JSON 处理 |
| Lombok | 1.18.30 | 代码生成 |
| Hutool | 5.8.25 | 工具库 |
| JUnit | 5.10.2 | 测试框架 |
| Maven | 3.8+ | 构建工具 |

### E. 参考资源

- **官方文档**: [README.md](README.md)
- **架构设计**: [doc/ARCHITECTURE.md](doc/ARCHITECTURE.md)
- **快速开始**: [doc/QUICKSTART.md](doc/QUICKSTART.md)
- **示例代码**: [evox-application/evox-examples](evox-application/evox-examples)
- **Spring AI 文档**: https://spring.io/projects/spring-ai
- **Spring Boot 文档**: https://spring.io/projects/spring-boot

---

## 📞 获取帮助

### 问题反馈

- **GitHub Issues**: https://github.com/your-org/evox/issues
- **讨论区**: https://github.com/your-org/evox/discussions
- **邮件**: evox-dev@example.com

### 社区支持

- 加入我们的开发者社区
- 参与每月的线上技术分享
- 关注我们的技术博客

---

<div align="center">

**感谢使用 EvoX！**

如果这个项目对你有帮助，请给我们一个 ⭐️ Star！

Made with ❤️ by EvoX Team

**文档版本**: 1.0.0  
**最后更新**: 2025-12-02

</div>
